# Пример файла конфигурации для backend
# Скопируйте этот файл в .env и заполните реальными значениями
# ВАЖНО: .env файл должен быть в .gitignore и НЕ должен быть доступен через веб-сервер

# Порт, на котором будет работать Express сервер
PORT=3000

# URL вашего frontend приложения (для CORS)
# Для локальной разработки обычно: http://localhost:5500 или http://127.0.0.1:5500
# Для продакшена: https://your-domain.com
FRONTEND_URL=http://localhost:5500

# Настройки PostgreSQL базы данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=todo_db
DB_USER=todo_user
DB_PASSWORD=ваш_надежный_пароль_здесь
